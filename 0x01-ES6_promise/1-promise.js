export default function getFullResponseFromAPI(success){
  const promise1 = new promise(true, false){
    if (true){
      return {status : 200, body :'success'};
    }
    else if(false){
      return('The fake API is not working currently')
    }
  }
}
