import ClassRoom from ./0-classroom.js

export default initializeRooms(){
  lists[19,20,34];
  value = [];
  for(list of lists){
    list = new Classroom(list)
    value.append(list);
  }
  return [value];
}
